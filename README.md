# Javier Barter — Academy Website

Premium ES/EN marketing site for **Javier Barter Jiu Jitsu Academy** in Panama City.

## Stack

- Next.js (App Router) + TypeScript + Tailwind CSS
- Locales: `es` (default), `en`
- Deploy target: Vercel

## Develop

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) (redirects to `/es`).

## Scripts

- `npm run dev` — local development
- `npm run build` — production build
- `npm run start` — serve production build

## Primary CTA

Free trial via WhatsApp: `https://wa.link/g2h8aa`
